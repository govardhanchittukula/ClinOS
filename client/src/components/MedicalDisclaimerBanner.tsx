import React from 'react';
import { AlertTriangle, ShieldAlert } from 'lucide-react';

interface Props {
  compact?: boolean;
}

export const MedicalDisclaimerBanner: React.FC<Props> = ({ compact = false }) => {
  if (compact) {
    return (
      <div className="bg-amber-950/40 border border-amber-500/30 rounded-lg px-3 py-2 text-amber-200/90 text-xs flex items-center space-x-2">
        <AlertTriangle className="w-4 h-4 text-amber-400 shrink-0" />
        <span>
          <strong>Clinical Support System:</strong> Outputs are AI-generated clinical support materials. <em>Not Medical Advice — Consult a Physician.</em>
        </span>
      </div>
    );
  }

  return (
    <div className="w-full bg-gradient-to-r from-amber-950/60 via-slate-900 to-amber-950/60 border-y border-amber-500/30 py-2.5 px-4 text-amber-200/90 text-xs shadow-inner">
      <div className="max-w-7xl mx-auto flex items-center justify-between">
        <div className="flex items-center space-x-3">
          <ShieldAlert className="w-4 h-4 text-amber-400 shrink-0 animate-pulse" />
          <p>
            <strong className="font-semibold text-amber-300 uppercase tracking-wider text-[11px] mr-2">
              Mandatory Disclaimer:
            </strong>
            ClinOS is an autonomous decision-support system. All outputs (triage ratings, differential diagnoses, care plans) are generated by multi-agent AI models for licensed practitioner evaluation. <strong>Not Medical Advice — Always Consult a Qualified Physician.</strong>
          </p>
        </div>
        <span className="hidden md:inline-block font-mono text-[10px] bg-amber-900/40 px-2 py-0.5 rounded border border-amber-500/40 text-amber-300">
          ISO-13485 COMPLIANT BOUNDARY
        </span>
      </div>
    </div>
  );
};
